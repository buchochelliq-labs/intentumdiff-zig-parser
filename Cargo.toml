[package]
name = "intentdiff-zig-parser"
version = "0.1.0"
edition = "2021"
description = "Zig parser plugin for IntentDiff (full-parse mode)"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
intentdiff-plugin-sdk = { git = "https://github.com/buchochelliq-labs/intentdiff-plugin-sdk", tag = "v0.1.0", features = ["ts-convert"] }
serde_json = "1"
tree-sitter = "0.25"
tree-sitter-zig = "1.1.2"
wit-bindgen = { version = "0.57", default-features = false, features = ["std", "macros"] }

[dev-dependencies]
intentdiff-plugin-sdk = { git = "https://github.com/buchochelliq-labs/intentdiff-plugin-sdk", tag = "v0.1.0", features = ["testing"] }

# Plugins target WASI p2 (Component Model); mirrors the monorepo's certified build profile.
[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"

# Build scripts run on the host; compile them simply so Windows AppControl doesn't block them
[profile.release.build-override]
opt-level = 0
codegen-units = 16
debug = false

# Patch out or stabilize build scripts (vendored from the monorepo's crates/patches).
[patch.crates-io]
quote = { path = "patches/quote" }
zmij = { path = "patches/zmij" }
serde_core = { path = "patches/serde_core" }
serde_json = { path = "patches/serde_json" }
wit-bindgen-rust-macro = { path = "patches/wit-bindgen-rust-macro" }
tree-sitter = { path = "patches/tree-sitter" }
tree-sitter-zig = { path = "patches/tree-sitter-zig" }
